# Lightweight-Feature-Flag-Service
A dev team wants to control which features are visible to which users without redeploying their application. They need a centralized service that client apps can query to determine what's enabled for a given user, with support for targeting rules and gradual rollouts.
